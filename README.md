# Compliance Intelligence

Scaffold for a compliance-focused RAG system (FastAPI backend + React frontend).

Quick start

Backend:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r backend/requirements.txt
uvicorn backend.app.main:app --reload --port 8000
```

Frontend:

```bash
cd frontend
npm install
npm start
```
